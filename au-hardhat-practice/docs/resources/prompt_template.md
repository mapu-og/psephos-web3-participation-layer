
Example Prompt for desinging
Implementation Plan - Python Todo App
Goal
Create a simple, functional, and aesthetically pleasing Todo List web application using Python (Flask).

Tech Stack
Backend: Python with Flask
Frontend: HTML5, CSS3 (Vanilla), Jinja2 templates
...

Add rules for code generation:
* The main method in main.py is the entry point to showcase functionality.
* Do not generate code in the main method. Instead generate distinct functionality in a new file (eg. feature_x.py)
* Then, generate example code to show the new functionality in a new method in main.py (eg. example_feature_x) and simply call that method from the main method.

add workflow for code generation:
* Generate unit tests for each file and each method
* Make sure the unit tests are named similar to files but with test_ prefix

https://codelabs.developers.google.com/getting-started-google-antigravity#8


Dictaminator: 
Actúa como un Desarrollador Frontend Senior experto en UX/UI moderna. Diseña una interfaz web (HTML/CSS/JS) siguiendo la estética "MapuriteLabs - Dictaminator Style". Sigue estrictamente estas reglas:

1. FUENTE Y TEXTO:
   - Usa la fuente 'Outfit' de Google Fonts.
   - Los títulos principales deben tener un degradado lineal de #f472b6 a #c084fc.
   - Las etiquetas de los formularios deben ser de 11px, bold, uppercase y con 1.5px de letter-spacing en color #94a3b8.

2. COLORES Y FONDO:
   - Fondo de página: Radial gradient desde #2e1065 (esquina superior derecha) hacia #0f172a (centro).
   - Color de acento primario: Rosa Mapu (#f472b6).
   - Color de fondo de tarjetas: rgba(255, 255, 255, 0.05) con backdrop-filter: blur(16px).

3. COMPONENTES CLAVE:
   - CARDS: Bordes redondeados de 28px, borde fino de 1px sólido rgba(255,255,255,0.1), y sombra suave de 32px.
   - BOTONES: Un gradiente de 135deg del rosa al púrpura, bordes de 14px, con un efecto de sombra dinámica y un levantamiento (transform: translateY) al pasar el cursor.
   - INPUTS: Fondo negro casi opaco (rgba(0,0,0,0.3)), bordes redondeados de 14px, y un focus que ilumine el borde en color rosa con un brillo exterior suave.

4. ANIMACIÓN:
   - Implementa una animación 'pulse' para el botón de 'Connect' usando box-shadow que se expande.
   - Añade transiciones suaves de 0.3s a todos los elementos interactivos.

5. ESTRUCTURA:
   - Usa un layout de Grid o Flexbox limpio y espacioso. Incluye un Footer minimalista con la marca "MapuriteLabs" usando un degradado de texto.

--------------------------------------------------------

   I am at week 7 of Alchemy university bootcamp. I would like to help me thorugh this by vibe coding. 
week-7-2-erc-20-governor
 
before procceding and for each step, consider that is for learning. be didactic (i am a visual visual learner). The idea is to learn. YO could add flowchats to the walkthrouh.
Let me know clearly what you neet for me (API keys, resources, etc).
Also create a way that we know everything is working as supposed. 
at the end update 
StudyNotes.md
 with the knowledge. I will use this for future development and security. 