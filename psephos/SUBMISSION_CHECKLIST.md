# Submission Checklist — Psephos (summary)

See the repository root `SUBMISSION_CHECKLIST.md` for the full reviewer checklist.

Quick product-specific notes for the reviewer:

- Product folder: `psephos/` (contracts, frontend, scripts, tests).
- Frontend root for Vercel: `psephos/frontend` — set the Vercel project root to this folder.
- Frontend build command: `npm run build` (in `psephos/frontend`).
- Start/preview: `npm start` after `npm run build`, or use `npm run dev` for local dev.

If you need me to, I can move untracked development files into `private_docs/` or add them to `.gitignore` to reduce Explorer noise.
